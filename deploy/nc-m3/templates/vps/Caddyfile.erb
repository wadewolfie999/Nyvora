# Combined root: preserve the protected package-managed Caddyfile first, then
# add the independently removable Node Control fragment.
import /etc/caddy/Caddyfile
import /etc/node-control/caddy/node-control.Caddyfile
